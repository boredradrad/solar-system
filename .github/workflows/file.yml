# optional
name: first wf

#pull_request
#workflow_dispatch
#push
on: push

jobs:
    first-job:
        # OS, must specify tag
        runs-on: ubuntu-latest
        # functions you'd like to run
        steps:
            - name: print my name
              run: echo "ITI"
